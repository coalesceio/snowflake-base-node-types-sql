fileVersion: 1
id: 43a14a5a-65c1-4372-afbf-d9d83c897590
name: WRK_NATION_REGION_JOIN
operation:
  config: {}
  database: ""
  deployEnabled: true
  description: Nation records joined with their region, combining nation and region attributes into a single table
  isMultisource: false
  locationName: TARGET
  materializationType: table
  metadata:
    appliedNodeTests: []
    columns: []
    cteString: |-
      WITH JON_CTE AS
      (SELECT
           N.*,
           R."R_NAME" AS "REGION_NAME" @description("Region name"),
           R."R_COMMENT" AS "REGION_COMMENT" @description("Free-text comment about the region"),
           'ABCD' AS CONSTANT
      FROM {{ ref('TARGET', 'WRK_NATION') }} N
      JOIN {{ ref('TARGET', 'WRK_REGION') }} R ON N."N_REGIONKEY" = R."R_REGIONKEY"
      )
    enabledColumnTestIDs: []
    sourceMapping:
      - aliases: {}
        customSQL:
          customSQL: |
            @id("43a14a5a-65c1-4372-afbf-d9d83c897590")
            @nodeType("704")
            @materializationType("table")
            @description("Nation records joined with their region, combining nation and region attributes into a single table")
            WITH JON_CTE AS
            (SELECT
                 N.*,
                 R."R_NAME" AS "REGION_NAME" @description("Region name"),
                 R."R_COMMENT" AS "REGION_COMMENT" @description("Free-text comment about the region"),
                 'ABCD' AS CONSTANT
            FROM {{ ref('TARGET', 'WRK_NATION') }} N
            JOIN {{ ref('TARGET', 'WRK_REGION') }} R ON N."N_REGIONKEY" = R."R_REGIONKEY"
            )
            SELECT * FROM JON_CTE;
            
        dependencies:
          - locationName: TARGET
            nodeName: WRK_NATION
          - locationName: TARGET
            nodeName: WRK_REGION
        join:
          joinCondition: FROM JON_CTE
        name: WRK_NATION_REGION_JOIN
        noLinkRefs: []
  name: WRK_NATION_REGION_JOIN
  overrideSQL: false
  schema: ""
  sqlType: "704"
  type: sql
  version: 1
type: Node
